# SPDX-License-Identifier: GPL-2.0-only
# Prevent USB modalias autoload. Explicit modprobe remains available for review;
# the installed controller uses an approved exact module path instead.
blacklist trigger6
